#!/bin/bash

# Gathers results for CPElide MICRO '24 artifact and emits in CSV format

# Babelstream

# Square

# Other benchmarks not included because of runtime for artifact, but concept
# is same as the above
